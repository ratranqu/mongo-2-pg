apiVersion: v1
kind: Service
metadata:
  name: source-mongodb
  labels:
    app: source-mongodb
spec:
  selector:
    app: source-mongodb
  ports:
    - name: mongo
      port: 27017
      targetPort: mongo
